import type { FC } from 'react';
import { Sidebar, SidebarHeader } from '../ui/sidebar';

export const AppSidebar: FC = () => {
  return (
    <Sidebar>
      <SidebarHeader />
    </Sidebar>
  );
};
